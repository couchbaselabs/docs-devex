= Configure the Cluster Connection 
:description: Learn how to obtain the connection string and set up the connection for the student cluster. 
:page-pagination: full
:page-topic-type: tutorial

[abstract]
{description}

== Cluster Connection

Before you can establish a connection to your cluster, you must obtain the connection string, add an allowed IP address, and configure the cluster access credentials.  

To configure the connection: 

. On the *Operational Clusters* page, select *student-cluster*.

. Go to menu:Connect[SDKs].

. Note the *Public Connection String*. This is required to connect to the cluster.

. Add an allowed IP address. 

.. Go to the *Allowed IP Addresses* page using the link on the screen.

.. Click btn:[Add Allowed IP].

.. On the *Add Allowed IP* page, select btn:[Add Current IP Address] to automatically populate your public IP address. 

.. Click btn:[Add Allowed IP].  
+
image::add-allowed-ip.png[alt="Add an allowed IP address"]
+

. Create cluster access credentials.

.. Go to the *Cluster Access* page using the link on the screen.

.. Click btn:[Create Cluster Access].

.. Enter an access name (e.g. `administrator`).

.. Enter a password (e.g. `Admin@123`). 

.. Under *Bucket-Level Access*, select *student-bucket* and *art-school-scope*, and set the access to *Read/Write*. 

.. Click btn:[Create Cluster Access].
+
image::create-cluster-credentials.png[alt="Create cluster access credentials"]

== Next Steps

After configuring the connection, you can xref:java-tutorial/install-couchbase-java-sdk.adoc[set up a Couchbase SDK and connect it to your cluster to write your first application].