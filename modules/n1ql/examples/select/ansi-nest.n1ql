SELECT r.airline, r.equipment, r.stops
FROM `<BUCKET_NAME>`.samples.route r
  NEST `<BUCKET_NAME>`.samples.airline a
  ON r.airlineid = META(a).id
WHERE r.sourceairport = "SFO"
AND r.destinationairport = "BOS";