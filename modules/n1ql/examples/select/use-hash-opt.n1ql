UPDATE STATISTICS FOR airline INDEX (def_samples_airline_name);
UPDATE STATISTICS FOR route INDEX (def_samples_route_route_src_dst_day);

EXPLAIN
/* tag::query[] */
SELECT COUNT(1) AS Total_Count
FROM route rte
INNER JOIN airline aline
ON rte.airlineid = META(aline).id;
/* end::query[] */