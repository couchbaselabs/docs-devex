SELECT 
    AVG(rev.ratings.Overall) AS Average,
    ROUND(avg(rev.ratings.Overall)) AS RoundedAvg,
    TRUNC(avg(rev.ratings.Overall)) AS TruncatedAvg
FROM hotel
UNNEST reviews rev
WHERE hotel.name = "Bacara Resort & Spa"