/* Data Control Language */

dcl-statement ::= grant
                | revoke

/* tag::grant[] */
grant ::= grant-user | grant-group | grant-consume-credentialstore
/* end::grant[] */

/* tag::grant-user[] */
grant-user ::= 'GRANT' role ( ',' role )* ( 'ON' keyspace-ref ( ',' keyspace-ref )* )?
          'TO' ( 'USER' | 'USERS' )? user ( ',' user )*
/* end::grant-user[] */

/* tag::grant-group[] */
grant-group ::= 'GRANT' role ( ',' role )* ( 'ON' keyspace-ref ( ',' keyspace-ref )* )?
          'TO' ( 'GROUP' | 'GROUPS' ) group ( ',' group )*          
/* end::grant-group[] */   

/* tag::grant-consume-credentialstore[] */
grant-consume-credentialstore ::= 'GRANT' 'CONSUME' 'CREDENTIALSTORE' credential-name ( ',' credential-name )*
          'TO' ( ( 'USER' | 'USERS' )? user ( ',' user )*
               | ( 'GROUP' | 'GROUPS' ) group ( ',' group )* )
/* end::grant-consume-credentialstore[] */

/* tag::revoke[] */
revoke ::= revoke-user | revoke-group
/* end::revoke[] */

/* tag::revoke-user[] */
revoke-user ::= 'REVOKE' role ( ',' role )* ( 'ON' keyspace-ref ( ',' keyspace-ref )* )?
           'FROM' ( 'USER' | 'USERS' )? user ( ',' user )*
/* end::revoke-user[] */

/* tag::revoke-group[] */
revoke-group ::= 'REVOKE' role ( ',' role )* ( 'ON' keyspace-ref ( ',' keyspace-ref )* )?
           'FROM' ( 'GROUP' | 'GROUPS' ) group ( ',' group )*
/* end::revoke-group[] */

role ::= identifier
user ::= identifier