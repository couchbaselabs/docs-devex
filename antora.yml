name: server
version: '7.6'
asciidoc:
  attributes:
    ui-name: Server Web Console
    product-name: Server
ext:
  preview:
    HEAD:
      sources:
        docs-server:
          branches: [release/7.6]
      override:
        startPage: server:introduction:intro.adoc
